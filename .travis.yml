language: node_js
node_js: 10.15.0
before_script: npm install -g firebase-tools
before_script: firebase use staging
deploy:
 provider: firebase
 token: $FIREBASE_TOKEN
 on:
   branch: master
 local_dir: www